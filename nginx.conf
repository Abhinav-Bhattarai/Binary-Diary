http {
    server {
        listen 80;
        listen 443 ssl;
        ssl_certificate cert.pem;
        ssl_certificate_key key.pem;

        location / {
            proxy_pass https://localhost:8000;
        }
    }
}

events { }