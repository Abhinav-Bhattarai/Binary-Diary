import React from "react";
import "./spinner.scss";

const Spinner = () => {
  return (
    <React.Fragment>
      <div className="loader">Loading...</div>
    </React.Fragment>
  );
};

export default Spinner;
